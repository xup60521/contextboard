{
  "name": "@contextboard/agent-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": { "contextboard": "./src/cli.ts" },
  "scripts": {
    "dev": "bun run src/cli.ts serve",
    "start": "bun run src/cli.ts serve",
    "build": "tsc --noEmit",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@contextboard/agent-tools": "workspace:*",
    "@contextboard/application": "workspace:*",
    "@contextboard/client-core": "workspace:*",
    "@contextboard/local-db": "workspace:*",
    "@contextboard/storage-indexeddb": "workspace:*",
    "@contextboard/sync-protocol": "workspace:*",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "fake-indexeddb": "^6.2.4",
    "typescript": "^6.0.2",
    "vitest": "^4.1.5"
  }
}
