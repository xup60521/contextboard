@AGENTS.md

## Exploration

When you need to broadly explore the codebase, shell out Haiku model to spawn codex process to do the work. 
The OpenAI models is capable and cheaper then Claude ones. When the scope is large, use `gpt-5.6-sol` on 
low reasoning level. When the scope is narrowed, use `gpt-5.6-luna` at xhigh.
