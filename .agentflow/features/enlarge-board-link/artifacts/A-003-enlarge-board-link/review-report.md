Outcome: BLOCKING — The review sandbox rejects every read-only command before execution (`bwrap: loopback: Failed RTM_NEWADDR`), so I could not inspect `517b7fe`, trace defaults/hydration, or run web-ui Vitest.

Minimality: BLOCKING — I could not verify the stated 20-line, three-file diff or account for added concepts directly.

Conformance: BLOCKING — I cannot substantiate that the frame is 480×256 across creation paths or that the flex layout removes the void. From supplied arithmetic only, the 320×152 floor yields a 272×112 inner area after 24px horizontal and 20px vertical inset; direct layout verification remains unavailable.
