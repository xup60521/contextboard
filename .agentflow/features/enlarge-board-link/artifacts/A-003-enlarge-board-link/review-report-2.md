Outcome: BLOCKING — the read-only shell cannot execute (`bwrap: Failed RTM_NEWADDR`), so I could not inspect `517b7fe` or verify paths/tests.

Minimality: BLOCKING — exact diff unavailable for review.

Conformance: BLOCKING — required direct cross-check could not be performed.
